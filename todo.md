# Things to include:
## Math
- PI as the value of acos(-1)

## Algorithms
- The complete reference for the SegTree
    - Recursive
    - Iterative
    - Lazy Propagation
- Prefix Sum Advanced
    - Min Stack
    - Monotonic Deque
    - Diff array
## Data Structures
- Ordered set
- Nuances of set and `unordered_set`
    - Own `set::lower_bound` and `set::upper_bound`
    - Multiset
## Language
- The header imports alternatives to `<bits/stdc++.h>`
- Set precision for cout
- How to correctly read whole lines
    - getline()
    - ignore()
- Vector max size for datatypes

# Format stuff
- ¿All code reference in one file, or a listing/file per function?
- Set the margins between the two columns so they don't overlap
- How to separate the thing into multiple TeX files so it's manageable

## Real format
- Make the code listings nicer
    - Some more syntax highlighting
    - The font kinda sucks
    - The comments are lost between the code text
